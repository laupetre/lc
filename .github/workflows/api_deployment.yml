name: Deploy API (Container Apps)

on:
  push:
    branches: [ main ]
    paths:
      - 'api/**'
      - '.github/workflows/api_deploy.yml'
  workflow_dispatch:

# ---- EDIT these defaults to your liking. Make sure ACR_NAME is globally unique (letters/numbers only). ----
env:
  AZURE_RESOURCE_GROUP: lc-swa-rg
  AZURE_LOCATION: eastus
  ACR_NAME: lcacr123456          # <--- change to a UNIQUE name (e.g., lcacr<yourrandomdigits>)
  CONTAINERAPPS_ENV: lc-swa-env
  CONTAINERAPP_NAME: lc-swa-api
  IMAGE_NAME: lc-swa-api
  OPENAI_MODEL: gpt-4o-mini

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set subscription (from AZURE_CREDENTIALS)
        run: |
          SUB_ID=$(jq -r '.subscriptionId' <<< '${{ secrets.AZURE_CREDENTIALS }}')
          az account set --subscription "$SUB_ID"
          az account show -o table

      - name: Check inputs
        run: |
          : "${AZURE_RESOURCE_GROUP:?Set AZURE_RESOURCE_GROUP in env}"
          : "${AZURE_LOCATION:?Set AZURE_LOCATION in env}"
          : "${ACR_NAME:?Set ACR_NAME in env (must be globally unique)}"
          echo "RG=$AZURE_RESOURCE_GROUP LOC=$AZURE_LOCATION ACR=$ACR_NAME"

      # ---------- Registry ----------
      - name: Ensure ACR exists
        run: |
          if ! az acr show -g "$AZURE_RESOURCE_GROUP" -n "$ACR_NAME" >/dev/null 2>&1; then
            az acr create -g "$AZURE_RESOURCE_GROUP" -n "$ACR_NAME" -l "$AZURE_LOCATION" --sku Basic
          fi
          az acr wait -n "$ACR_NAME" --created
          az acr show -g "$AZURE_RESOURCE_GROUP" -n "$ACR_NAME" -o table

      - name: ACR login (AAD)
        run: az acr login -n "$ACR_NAME"

      - name: Build & push image
        uses: docker/build-push-action@v5
        with:
          context: ./api
          push: true
          tags: ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest

      # ---------- Container Apps ----------
      - name: Setup Container Apps
        run: |
          az extension add --name containerapp --upgrade
          az provider register --namespace Microsoft.App --wait
          az containerapp env create -g "$AZURE_RESOURCE_GROUP" -n "$CONTAINERAPPS_ENV" -l "$AZURE_LOCATION" || true

      - name: Cr
